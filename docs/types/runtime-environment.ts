enum RuntimeEnvironment { // eslint-disable-line no-restricted-syntax
  Development = 'development',
  Production = 'production',
}

export default RuntimeEnvironment; // eslint-disable-line import/no-default-export
